runtime: python310
instance_class: F1

entrypoint: uvicorn app.main:app --host 0.0.0.0 --port $PORT

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

env_variables:
  ENVIRONMENT: "production"
